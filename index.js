const { cricket } = require('./cricket/data.js')
/*
class cricket {
    constructor(match_url = '') {
        this.match_url = match_url
    }
    async match_url() {
        return await cricket.match_url()
    }
    async live_score(match_url) {
        return await cricket.live_score(match_url)
    }
    async match_news() {
        return await cricket.match_news()
    }
    async feature_match() {
            return await cricket.feature_match()
    }
    async upcoming_match() {
            return await cricket.upcoming_match()
    }
    async recently_match() {
            return await cricket.recently_match()
    }
}
*/
module.exports = cricket
